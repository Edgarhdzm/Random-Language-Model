# Random-Language-Model
Implement the Random Language Model (RLM) in PyTorch and train neural networks on the RLM dataset. The RHM is a synthetic multiclass classification dataset inspired by the hierarchical compositionality of natural data.
